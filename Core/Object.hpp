#pragma once

namespace CYOND {
	class ObjectReference;
	//! @brief Anything that needs to be allocated in the heap (Dynamic lists/strings, datums with unscoped life)
	class Object {
	private:
		//ObjectReference* FReferences;
	};
};